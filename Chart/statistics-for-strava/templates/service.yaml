apiVersion: v1
kind: Service
metadata:
  name: {{ include "statistics-for-strava.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "statistics-for-strava.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080